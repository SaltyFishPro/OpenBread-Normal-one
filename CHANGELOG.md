# OpenBread Normal One 更新日志

这里记录每次更新里你能看到、能感觉到的变化。

## 20260923

- 主菜单新增「api统计板」入口和动态图标，目前显示“详情内容开发中”，暂不查询用量或联网刷新。

## 20260922

- 专注计时页右上角新增当前时间，方便随时看几点；目标时长移到大数字下方，持续任务同时显示累计时间。

## 20260921

### 新增

- 弹窗出现和消失都带动画了：从屏幕上方滑下来并轻轻放大，确认或取消后反向收回，整个过程约 0.1 秒，不拖沓。
- 专注时钟从"选卡片"进入"计时界面"不再是瞬间切换：卡片向上滑出、计时页从下方滑入；放弃专注返回卡片列表时同样有过渡。
- 主菜单新增「闹钟」入口，带动态铃铛图标（目前点进去还是占位页，闹钟功能随后补上）。
- 主菜单新增「课程表」入口，带动态日历图标（同样是占位页，课程表功能随后补上）。

### 改进

- 音量提示弹窗也带上了同样的进出动画，按 Left 或 OK 后它会自己收起来。
- 弹窗期间按键会被动画流程吃掉，连按不会误触发后面的页面操作。

## 20260920

### 新增

- 专注时钟现在真的能计时了：选好卡片和时长按 OK 开始；持续任务会自动按「专注 + 休息 5 分钟」循环三轮，短专注、长专注专注一次就结束。计时中可以暂停，完成后可以直接再来一次。
- 设置里新增「USB 串口」开关，可以关掉电脑上识别到的调试串口。
- 设置里的「恢复默认设置」现在能用了：会清空全部设置（WiFi 账号、串口开关等）并重启。

### 改进

- 明显更省电：主界面待机电流从约 80mA 降到 50mA 左右，浅睡时约 25mA。空闲时会自动降低处理器频率，画面静止时也不再反复整屏重画。
- 更流畅：菜单滑动、图标动画、时间卡片只重画变化的部分，切换和滑动更连贯。
- 弹窗操作统一了：左边是取消（默认选中），右边是执行项；Left 一律是取消，OK 是确认。「重启设备」「恢复默认设置」「应用固件」「开始配网」「放弃专注」都用同一套外观和按键。
- 专注时钟的选择界面更顺手：卡片切换会整张滑出屏幕，选项胶囊的文字不再顶到边框，去掉了多余的底部提示，四周留白统一。

### 修复

- 修复音乐列表里左右切换标签时，底部导航栏的动画跑到屏幕顶部的问题。
- 修复主界面每秒多一次无意义重画的问题（表现为画面轻微卡顿、处理器频率每秒来回跳）。
- 修复「重启设备」弹窗里 Left 会选中"是"的问题，现在 Left 就是取消。

## 20260916

- 时间显示的条件放宽：只要 RTC 能读到时间就显示，不再要求日期在合理范围内、也不要求之前校准过，避免校时成功后主界面仍显示"时间需校准"。
- RTC 读取失败时继续显示小面包和提示，读取恢复正常后自动恢复时间显示。
- 月份或星期超出标签范围时显示 `---`，不再影响时分显示。

## 20260913

- 修复未校准时主界面偶尔显示出 RTC 里残留旧时间的问题；这一版改为"只有校准过、且 RTC 数据正常才显示时间"。
- 时间无效时的提示改成会动的小面包动画，主界面不再显得空。

## 20260912

- 主界面新增 20 秒无操作自动休眠：进入浅睡但保留画面，按任意键唤醒；休眠期间由 RTC 每分钟唤醒一次刷新时间。
- 时间校准移到设置菜单，放在 WiFi 配网下方；配网成功后会自动校时一次，失败可以进设置重试。
- 修复首次配网或校时完成后无法进入自动休眠的问题。

## 20260908

- 完善设备自检：屏幕、按键、I2C、RTC、SD 卡、IMU、电量计都能在设备上直接测。
- 关闭 RTC 未使用的时钟输出以省电；移除浅睡测试与蓝牙校时。
- 统一 I2C 总线恢复流程，修复传感器与 RTC 相互影响的问题。

## 20260906

- 界面固定使用中文，去掉语言切换；校时只保留网络校时。
- 设置里新增设备自检，左侧选项、右侧说明，操作统一为 Up/Down 选择、OK 确认、Left 返回。
- 完成屏幕、按键、I2C、RTC、SD 卡、IMU 测试页面，并新增电量与电源状态测试。
- 硬件测试按需运行，诊断日志可以关闭。

## 20260524

- 修复单词阅读翻页卡顿，长释义显示更顺畅，版面布局也重新调整过。
- 单词库改为索引读取，翻页更快。
- 适配 ESP32-S3 N16R8（16MB Flash / 8MB PSRAM）。

## 20260511

- 新增单词阅读：8 个词库入口，进入后是单词页，Left 返回。
- 设置里新增「USB 挂载 SD 卡」，挂载期间设备不访问 SD 卡，避免和电脑同时读写。
- 新增词库补全工具，支持联网补全和音标格式统一。

## 20260509

- 完成音乐播放器界面：底部按钮栏、播放页、律动区、进度条、歌词和耳机/扬声器状态。
- 支持播放/暂停、上一首/下一首、随机与顺序播放、音量调节。
- 修复 FLAC 时长识别异常导致无法播放、部分 MP3 卡住的问题，音乐列表滚动也更流畅。

## 20260504

- 更新音乐文件列表界面与操作逻辑，加入音量、文件详情和收藏控制。
- 硬件：PCB 已验证通过。

## 20260503

### 软件

- 设置里新增「IIC 设备检测」。
- 为音乐功能做准备，优化状态机、FLAC 解码与低功耗/RTC 读取调度。

### 外壳

- 挂绳孔验证失败，计划取消。
- 新增"猫耳朵"外壳样式，也可以选择不带猫耳朵的版本。
- 四边螺丝改为 M1.7。
