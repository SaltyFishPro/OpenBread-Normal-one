# OpenBread Normal One 更新日志

## 20260920

### 专注时钟卡片界面

1. 一级页面选中「专注时钟」后按 OK 直接进入专注时钟页面，跳过多余的单条目二级目录；Left 直接返回一级首页，并补充对应的进入与返回过渡动画。
2. 专注时钟页面由圆弧列表改为卡片堆叠界面：持续任务、短专注、长专注三张卡片，分别提供 10/15/20、10/15/20、30/60 分钟时长；Up/Down 切换卡片，Right 切换时长，OK 暂为预留。
3. 卡片切换时整卡滑出屏幕边界并在边缘裁切，不再因裁剪而压缩变形；时长胶囊焦点改为快速缓出，连按时从当前动画位置接续。
4. 选项胶囊的高度、圆角与左右内边距按中文字体实际 ascent/descent 重新计算，文字不再顶到边框；移除底部操作提示，卡片在屏幕四周统一留 20px 边距。
5. 移除卡片不可见的白色阴影与 3 张静止背景卡片，显著降低动画每帧的像素写入量。

### UI 代码重构与屏幕局部刷新

1. 新增 `ui/AnimMath.h`，将分散在 UiManager、HomePage、MusicPage、FocusClockPage 的缓动与插值公式收敛为一份实现，保留原有的截断与四舍五入两种取整语义，动画数值不变。
2. 新增 `ui/DrawUtils.h`，统一裁剪矩形、圆角实心矩形和扫描线圆角矩形绘制；UiManager 与 ReaderPage 中逐字节重复的圆角实心实现合并为一份。
3. 新增 `ui/DetailHeader.h/.cpp`，替换时间校准、敲木鱼、蓝牙、设置四个页面各自复制的详情页标题栏。
4. 清理 20260906 删除语言切换功能时遗留的双语文案：移除 `HomePage::Language` 枚举、各页面的 `language` 参数、全部 `...En()` 函数、`labelEn` 字段及英文分支。Flash 占用减少约 7.5 KB（3745571 → 3737927 字节）。
5. 让 `Render1bpp` 的脏区域真正生效：ST7305 驱动新增窗口式写入 `displayRegion`，`DisplayMonoTft` 增加逻辑坐标的 `presentRegion`，音乐导航栏与列表动画改为只推送脏区域。导航栏动画单帧 SPI 数据由 8064 字节降至约 2880 字节（约 64%）。该路径仍待实机确认，可用 `OB_PARTIAL_REFRESH_ENABLED=0` 回退整屏刷新。

### 功耗优化与蓝牙资源回收

1. 主循环按子系统分级让出 CPU。原先 `loop()` 没有任何延时，而 ESP32 Arduino 核心的 `loopTask` 是无延时死循环，其 `yieldIfNecessary()` 只在单核配置下生效，双核的 ESP32-S3 因此会让 CPU 以 240MHz 满速空转。现在真正空闲时按 `kInputTickMs`（10ms）轮询，配网/OTA/蓝牙/硬件自检等协作式子系统最多让出 1ms，动画按剩余帧时间让出，音频播放保持原有调用节奏不变。
2. 主界面未校准小面包动画改为局部刷新。原先 220ms 一帧都会重绘整屏背景、菜单图标、时间与日期卡片，并整屏推送 8064 字节；现在只重绘时间卡片区域（逻辑坐标 15,15–140,60），单帧 SPI 数据约 967 字节，降幅约 88%。
3. 蓝牙远程功能结束后真正释放协议栈。原先 `cleanupStack()` 只复位标志并保留 BLE 栈，`new` 出来的 security/server/callbacks/hid 从不释放，`BLEDevice::deinit()` 也从未调用，蓝牙控制器长期常驻。现在按「先 `BLEDevice::deinit(true)` 回收 server/client 与服务特征，再释放自有对象」的顺序回收。
4. `IconBitmap::drawFrame` 去掉逐像素除法。原先内层循环每个像素都要算一次 `dx * 源宽 / 目标宽`，主界面背景 386×180 缩放到 384×168 时单帧就是 64512 次 32 位软件除法（ESP32-S3 无硬件除法器，每次几十周期）。改为累加步进推导源索引，取值序列与原来完全一致，画面不变，单帧省下约 5–10ms。该改动已用 1584 组尺寸/裁剪/反色/RLE 组合对拍验证像素调用序列完全相同。
5. 主界面菜单图标动画改为只刷新图标区域。主界面背景是静止单帧，但原先每次图标换帧都会整屏重绘（64512 像素）。现在菜单静止时只重绘 6 个图标所在的 `(196,10)-(237,160)` 区域，像素量降到 6342，单帧 SPI 数据由 8064 字节降到约 882 字节，降幅约 90%。菜单绘制循环抽成 `drawMenuWheel()`，整屏路径与局部路径共用同一份布局逻辑，避免以后改动时两边不一致。
6. 局部刷新仅在「轮盘布局与上一次整屏渲染一致」时启用。滑动结束那一帧 `update()` 会先更新焦点索引并把偏移归零，此时局部刷新无法更新焦点框与标签，会出现焦点框滞后、旧图标从 42px 缩小到 32px 后残留外圈的问题。现在记录上次整屏渲染的焦点索引与纵向偏移，布局变化时强制整屏重绘一次，之后才恢复局部刷新。
7. 新增空闲降频策略。此前即使 UI 完全静止，CPU 仍以 240MHz 空转。现在新增 `bsp/CpuClock`：默认满速，只有完全没有渲染、过渡、音频、射频与自检时才降到 80MHz；满屏渲染前会先把主频拉回 240MHz 再绘制，避免整帧在降频状态执行。下限取 80MHz 而非更低，是因为 ESP32-S3 在 CPU=80MHz 时 APB 仍为 80MHz，SPI/I2C/I2S/UART 的分频不变；切换时输出实际频率与 APB 频率便于核对。新增功能需要满速时调用 `CpuClock::setBoost()` 申请对应理由位；`OB_CPU_IDLE_SCALING_ENABLED=0` 可回退为常驻 240MHz。

### 设置新增 USB 串口开关

1. 设置菜单新增「USB 串口」项，可主动开启或关闭芯片内置的 USB Serial/JTAG（HWCDC）串口。关闭时 `HWCDC::end()` 会释放收发缓冲与中断、把 D+/D- 拉低让主机看到设备断开；此后各模块日志的 `if (!Serial)` 判断自动为假，写操作也退化为安全空操作，不需要改动任何日志函数。
2. 开关状态写入 NVS 并跨上电保持。注意芯片复位后 USB Serial/JTAG 外设本身处于激活状态，仅「不调用 `begin()`」并不会让主机看到断开；因此开机读到关闭状态时会先 `begin()` 注册 USB D+/D- 引脚、再 `end()` 主动断开。关闭状态下开机不再有 300ms 的枚举等待。
3. 串口初始化从 `main.cpp` 移到 `App::begin()`，统一由 `bsp/UsbSerial` 管理；设置菜单因此增加为 9 项，设备自检之后的索引已同步调整。
4. 实测结论：关闭 USB 串口后主界面浅睡电流仍约 25mA，与开启时无明显差异，说明该电流并非 USB CDC 造成，后续需从显示 HPM 模式与各 LDO 供电方向继续排查。

### 主界面滑动渲染优化

1. 新增主界面静态图层缓存。背景、时间卡片与日期卡片在时间有效且无横向偏移时渲染一次后整帧快照到 8KB RAM，之后每帧直接整帧回填，不再逐像素重画 64512 点的全屏背景；时间未校准（小面包每 220ms 换帧）或页面过渡时自动退回实时绘制，时间卡片的时/分/日期变化时缓存失效重建。
2. 滑动期间只推送菜单列。轮盘滑动时只有右侧菜单列在变，静态图层回填后其余区域与屏幕内容一致，因此只把菜单列（逻辑 x 186–383、y 10–160）推给面板，单帧 SPI 数据由 8064 字节降到约 4165 字节；若该帧静态图层是重建而非回填，则自动退回整屏推送。
3. 修复每秒一次的多余整屏重绘。`TimeService::updateSnapshotTime()` 原先用 `memcmp` 比较整个时间结构体，秒一变就标记 changed，导致主界面每秒重绘一次——串口日志里 CPU 频率在 80/240MHz 之间每秒来回跳正是这个原因。现在快照仍会更新秒值，但只有时/分/日期/有效性变化才触发重绘；自检页的秒显示走独立的 `RtcTestService`，不受影响。

### 线段绘制与圆角实现优化

1. ST7305 驱动改按物理字节批量写入线段。基类的 `drawFastHLine` / `drawFastVLine` 原本是逐像素调用 `writePoint`（每像素都要做旋转换算与位运算），现在改为先在逻辑坐标裁剪，再把线段映射成一条物理水平线或竖直线，按帧缓冲的字节粒度合并写入——水平段一次最多覆盖 4 个像素、竖直段一次覆盖 2 个像素。所有文字绘制与圆角矩形最终都走这两个原语，因此全界面受益。已按 4 种旋转 × 23232 组参数（含负坐标、越界、零长度）与旧实现逐字节对拍，结果一致。
2. MusicPage 的第三份圆角实现合并进 `DrawUtils`。同时把 `DrawUtils::drawRoundRect` 的半径钳制由 `(边-1)/2` 调整为 `min(宽,高)/2`，使胶囊端部成为完整半圆；改动前只有 MusicPage 的调用点会触发半径饱和，其余调用点半径都远小于上限，因此合并后所有调用点的像素与之前完全一致。

### 文本助手与居中算式去重

1. 新增 `ui/TextUtils.h`，把 ReaderPage 的 `utf8CharLen` 与 MusicPage 的 `utf8CharCount` / `suffixByUtf8Chars` 合并为一份实现，续字节判定收敛为唯一的 `utf8IsContinuation`。MusicPage 原判定写作 `(c & 0xC0) != 0x80`（char 提升后按有符号比较），已逐字节核对 0–255 全部取值与新的无符号判定结果完全一致；其余循环与返回条件逐字对照原实现。
2. 文本居中收敛为 `centeredTextX`（按中心点居中）与 `centeredTextXInBox`（按方框居中）两个助手，替换 8 个文件中 29 处手写的 `中心点 - 宽/2`、`框左 + (框宽 - 宽)/2` 算式。替换只把算式换成同名调用，助手内部即原表达式，因此渲染坐标逐像素不变；ReaderPage 卡片文字保留原有的 `x+2` 保底缩进，各处右对齐、滚动与焦点框宽度计算不属于居中，保持原样。
3. 仍有一处未纳入：DeviceSelfTestPage 的 `renderSdTest` 整个函数被压缩在一行（含中文字面量约 1000 字符），无法安全做行级替换，留待后续拆分重构。
4. 占用变化：Flash 3749639 → 3749531 字节（减少 108 字节），RAM 75720 字节不变。

### 图标 RLE 解码缓存

1. 新增图标整帧解码缓存（`IconBitmap`，8 槽 × 1300 字节）。原实现按输出行逐行解码，每一行都要从游程流起点重新扫描到该行：主界面菜单一帧的 4 个 RLE 图标合计约 2.8 万次游程扫描，而像素写入只有约 6.9 千次，游程扫描才是实际瓶颈。现在整帧解码一次（约 1.6 千次扫描）后按行偏移索引，直到该图标动画帧号变化才重新解码；超过槽位容量的图（150×150 子菜单图标 2850 字节、全屏背景 8685 字节）仍走逐行解码兜底，行为与之前一致。
2. 验证方式：离线对拍整帧解码与逐行解码，覆盖 6 个真实图标资源共 10650 行，结果逐字节一致。首版把槽位写成 1280 字节，小于 100×100 图标解码后的 1300 字节（13 字节/行 × 100 行），菜单图标会全部落到兜底路径、缓存从未命中；对拍时发现并修正为 1300，现已确认 4 个 100×100 菜单图标命中缓存、150×150 图标按预期走兜底。
3. 占用变化：RAM 75720 → 86192 字节（+10472，来自 8 槽缓存），Flash 3749531 → 3749791 字节（+260）。

### 音乐导航与列表动画退回整屏推送

1. 音乐页的导航栏动画与列表焦点动画原本使用「部分列 + 全行」的局部刷新窗口，实机确认在 ST7305 上窗口映射会错位，表现为左右切换标签时导航栏动画被画到屏幕顶部（底部只剩图标、顶部出现带文字的导航栏），随后自行恢复。现将这两类动画改回整屏推送；主页的局部刷新（时间卡片、菜单图标）保持不变，稳态功耗不受影响。
2. 新增 `[MUSIC]` 动画触发日志（`OB_MUSIC_LOG_ENABLED` 控制），记录导航标签切换与进入/退出播放器动画的触发点，便于排查这类窗口映射问题。

### 专注时钟计时界面

1. 卡片选择界面按 OK 即开始计时。全部按「已用时间正计时」累加：持续任务 =（专注 + 休息 5 分钟）× 3 轮，短专注 / 长专注 = 一次专注后结束；专注时长取各卡当前选中的选项（持续任务、短专注为 10/15/20 分钟，长专注为 30/60 分钟）。
2. 计时界面布局：顶部黑条左侧显示卡片名与阶段（持续任务显示「持续任务 · 专注 1/3」，休息阶段显示「休息 n/3」），右侧显示目标时长或「已暂停」；中间是 46×74 的 Segment7 大字已用时间；下方一行小字显示累计时长（持续任务）或目标时长，最下方是本阶段进度条。
3. 按键：OK 开始 / 暂停 / 继续，完成页 OK 再来一次；Left 在计时中弹出「放弃本次专注？」确认框（默认停在「继续」，Right/Up/Down 切换选项，OK 确认，Left 关闭弹窗并继续计时），在选择界面与完成页 Left 仍是返回主界面；运行界面 Up/Down 无功能。
4. 刷新策略：只在显示的秒数变化时重绘，开始 / 暂停 / 阶段推进 / 完成等状态切换整屏重绘。阶段推进是自动的，进入休息或下一轮时从 00:00 重新正计时。
5. 计时状态目前保存在页面内（FocusClockPage），离开页面即结束；从主界面进入页面会复位到卡片选择界面。后续若要「离开页面继续计时」或「主界面显示专注中」，再把状态机挪进 services。
6. 占用变化：RAM 86192 → 86216 字节（+24），Flash 3750103 → 3752751 字节（+2648）。

## 20260916 RTC 时间显示条件简化

1. 按新的显示策略，RTC 初始化成功且日期时间读取成功即显示时间；取消日期范围、时钟完整性标志和历史校准记录对显示状态的限制，替代 20260913 的严格判定策略。
2. RTC 不可用或日期时间读取失败时继续显示小面包及提示；读取恢复成功后恢复时间显示。此策略不保证读到的时间准确，也不屏蔽通信失败造成的状态切换。
3. 月份、星期超出标签范围时显示 `---`，避免日期卡片数组越界，不影响时分显示。校准写入的输入校验继续保留。
4. 增加读取失败和恢复时的原因日志，沿用 `OB_TIME_LOG_ENABLED` 开关；不增加后台联网或待机唤醒频率。

## 20260913 主界面未校准状态与时间有效性修复

1. 修复未完成时间校准时，RTC 中残留的合法日期可能被误判为有效时间并间歇性显示的问题；现在只有成功校准、RTC 数据合法且时钟完整性正常时才显示时间。
2. 新增时间有效状态变化日志，便于区分未校准状态与 RTC/I2C 瞬时读取异常。
3. 将主界面时间无效状态替换为 21×21 的四帧小面包动画资源，使用 220ms 帧间隔。
4. 小面包动画按 2 倍最近邻方式显示为 42×42，保持像素风格并在时间卡片内居中。

## 20260912

### 主界面自动休眠与 RTC 分钟唤醒

1. 主界面新增 20 秒无操作自动休眠：进入 light sleep 并保留屏幕画面，关闭传感器 LDO、卸载 SD 卡，关闭 RTC_PERIPH 与 MODEM 电源域。
2. 按键唤醒采用任意按键 GPIO 低电平唤醒，唤醒后恢复显示、传感器和按键去抖状态。
3. 新增 PCF85063A 分钟中断：进入休眠前启用 MI 并关闭 HMI/AIE，GPIO7 INT 低电平唤醒后刷新主界面时间并继续休眠。
4. 新增 RTC Timer Flag 清理接口，避免 INT 停在低电平；唤醒时区分按键唤醒与 RTC 分钟唤醒。
5. RTC 不可用时仍保留按键唤醒休眠，但睡眠期间不刷新时间。

### WiFi 配网与自动休眠修复

1. 修复首次 WiFi 配网和网络校时完成后无法进入主界面自动休眠的问题：配网成功后关闭 WiFi 射频，休眠判断改为检查无线会话是否仍处于活动状态，同时保留已保存的配网状态。

### 时间校准与配网流程调整

1. 将时间校准从时钟菜单移至设置菜单，位于 WiFi 配网下方；同步调整设备自检、关于设备等菜单索引。
2. 每次主动热点配网成功后自动执行一次网络校时，复用当前 WiFi 连接；不在开机时自动联网，也不定期联网校时。
3. 配网页显示自动校时进度与结果。校时失败仍保留配网信息，可进入设置中的时间校准重试；Left 取消校时并返回。
4. 校时完成、失败或取消后停止 SNTP 并关闭 WiFi，保留主界面 20 秒自动休眠；校时须收到本次 NTP 响应，避免把旧系统时间误判为成功。

## 20260908 OpenBread Normal One 更新日志

1. 完善设备自检：屏幕、按键、I2C、RTC、SD 卡、IMU 与电量计测试页面及驱动。
2. RTC 关闭未使用的 CLKOUT；移除浅睡眠测试和蓝牙校时。
3. 统一 I2C 总线恢复流程，排查传感器 LDO 关闭时 QMI8658A 对共享总线的影响。

## 20260906 OpenBread Normal One 更新日志

1. 删除语言 切换功能，界面固定使用中文；时钟校时仅保留网络校时。
2. 设置中新增设备自检页面，采用左侧选项、右侧简介布局，并统一 Up/Down、OK、Left 操作。
3. 完成屏幕、按键和 I2C 测试，优化中文样张行距、动态刷新率显示和测试项提示。
4. 完成 PCF85063A RTC、SD 卡和 QMI8658A IMU 测试，支持分页显示检测状态及实时数据。
5. 新增 MAX17048 驱动与电源测试，显示电量、电压、变化率、芯片信息及音频/传感器 LDO 状态。
6. 电源自检删除浅睡和深睡流程，所有硬件测试按需运行并提供可关闭的串口诊断日志。

## 20260524 OpenBread Normal One 更新日志

1. 修复单词阅读翻页卡顿问题，减少静止页面的重复刷新。
2. 优化单词释义绘制逻辑，长释义显示更顺畅。
3. 调整单词阅读界面布局，标题、音标、释义和分割线位置更合理。
4. 适配 ESP32-S3 N16R8 板卡，启用 16MB Flash 和 8MB PSRAM 配置。
5. 单词库读取改为建立词条索引，翻页时按位置读取，不再从头扫描。
6. 清理调试阶段留下的日志和临时缓存代码。

## 20260511 OpenBread Normal One 更新日志

1. 新增单词阅读规划：确定 SD 卡 words 目录词库方案。
2. 新增词库补全工具：支持 GUI、进度显示、主词典匹配、联网补全、多源查询，音标统一为 [音标]。
3. 新增单词库 UI：8 个词库入口，选中实心显示。
4. 新增单词显示页：单词、释义、底部栏、上下指示区域，并接入 LEFT 返回。
5. 设置页新增 USB挂载SD卡 功能。
6. USB 挂载改为前台独占：进入功能挂载，退出功能停止挂载。
7. USB 挂载期间暂停设备端 SD 访问，避免和电脑同时读写冲突。
8. 切换 USB 到 TinyUSB OTG 模式，支持 USB MSC。

## 20260509 OpenBread Normal One 更新日志

1. 完成音乐播放器 UI 主体适配，包括底部 Button Bar、播放页侧边栏、律动区、进度条、歌词区与耳机/扬声器状态指示。
2. 接入音乐播放基础逻辑：播放/暂停、上一首/下一首、随机/顺序、音量弹窗调节。
3. 优化音乐列表与 Button Bar 绘制策略，降低刷新开销，滚动动画更流畅。
4. 修复 FLAC 时长识别异常导致无法播放的问题。
5. 修复部分 MP3 无法稳定解码时无限卡住的问题，增加同步头偏移修正与无 PCM 输出保护。

## 20260504 OpenBread Normal One 更新日志

1. 更新音乐文件列表管理界面，包括动画效果，整合了操作逻辑。
2. 更新音乐文件列表管理界面的硬件控制，包括音量、文件详情、音乐收藏。
3. 硬件方面正在写立创的开源文档，PCB 已经验证通过。

## 20260503 OpenBread Normal One 更新日志

### 软件方面

1. 新增“设置”内的“IIC设备检测”。
2. 优化状态机状态，为后续音乐功能做准备。
3. 优化了解码库关于 FLAC 的解码。
4. 低功耗控制 LDO 状态，优化 RTC 读取调度休眠策略。

### 外壳方面

1. 挂绳孔验证失败，准备砍掉。
2. 新增版本猫耳朵外壳样式，可选择没有猫耳朵的样式。
3. 四边螺丝尺寸重新选择 M1.7，直径 4mm 长度螺丝。
